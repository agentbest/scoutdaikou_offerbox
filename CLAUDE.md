# scoutdaikou_offerbox — OfferBox運用代行LP（採用企業向け・B2B）

- 公開URL: https://scoutdaikou-offerbox.agent-best.net/ （GitHub Pages、`.nojekyll` ＋ `CNAME`）
- サービス: **OfferBox運用代行（完全成果報酬型）**。新卒採用したい企業向け。
- アクセント色: ティール `#0D7E93`
- リポジトリ構成は `index.html` / `CNAME` / `.nojekyll` / `README.md` / `.gitignore` ＋ **メディア「地方採用ラボ」（`media/` と生成器 `tools/media/`）**。以前同梱されていた microCMS の Next.js ブログテンプレート一式は**未使用のため2026-07-25に削除済み**。戻さない。

## 訴求の軸

- **「成果が出なければ、費用は0円」＝ 完全成果報酬型（リスクゼロ）** が信頼の軸。
- **AI × RPO（採用代行）／ AI × スカウト代行**。AI活用で精度高く運用できる、を強調。
  ⚠ **「APO」は誤記。正しくは RPO。**
- スタートアップ導入事例（「何から始めればいいか分からない状態から母集団づくりを支援」）を先頭に。

## 保持している事実情報

- 料金 **20万円/1名**（入社承諾時）
- 返金規定: **入社前の退職（内定承諾後の辞退）のみ100%返金**。入社後の返金段階は2026-09-10に廃止（松岡さん指示）
- OfferBox統計: 2027卒 25万人+・累計23,106社（OfferBox公式サイト、2026年7月時点）・開封率82%・承認率15〜20%（※旧公表値。現行サイトに数値掲載なし）
- 会社概要: 設立2020/4/1、港区六本木4-8-7 嶋田ビル5階、有料職業紹介 許可番号 **13-ユ-316964**。ミッションは「日本に、磁力を生む。」（旧「デジタルディバイド…」は2026-09-10に差し替え済み）
- 代表紹介セクション（08）あり。経歴・専門領域はコーポレート /profile/ と scout LP に合わせる

## デザイン

**白ベース（ダークモード無効）** ＋ネイビー×ティール。トーンは信頼感重視。
緑 `#12A150` は「0円・成果報酬・返金」など**お金／成果のシグナル専用**（green LPのアクセント色とは用途が違う）。ヒーロー／最終CTAはネイビー帯を維持。

## CTA の経緯

かつては Timerex `https://timerex.net/s/r_matsuoka/6666f3b5/` へ直リンクしていたが、**2026-08-11のB2B窓口ルール統一で、ページ内フォーム受け＋サンクス画面に日程調整リンク、に変更済み**。直リンクに戻さない。

## メディア「地方採用ラボ」（/media/）— 2026-09-12 立ち上げ

地方の中小・中堅企業の採用担当者に向けた**新卒採用ノウハウメディア**。記事2,000本。
LPと同じドメイン配下（`https://scoutdaikou-offerbox.agent-best.net/media/`）に置いて、
記事からOfferBox運用代行の相談導線（`/#entry`）へ送る。

### ⚠ ビルドフロー（最重要）

**`media/` 配下・`sitemap.xml`・`robots.txt` はすべて生成物。直接編集しない。**
編集するのは `tools/media/` の定義ファイルで、そのあと生成器を回す。

```
tools/media/*.mjs を編集
  → node tools/media/generate.mjs   （media/ 2,138ページ ＋ sitemap.xml / robots.txt を再生成。数秒）
  → ブラウザで確認 → commit & push  （GitHub Pages に即反映）
```

| ファイル | 役割 |
|---|---|
| `tools/media/prefectures.mjs` | 47都道府県の基礎データ（地域ブロック・県庁所在地・主要都市・主要産業・主要大学・県ごとの note） |
| `tools/media/hubs.mjs` | ハブ（カテゴリ）131個の定義。グループ5・記事本数もここ。**合計がちょうど2,000本になるようにしている** |
| `tools/media/hub-points.mjs` | 課題・スカウト採用・テーマの各ハブ固有の「見るべき観点」。チェックリストと本文の両方で使う |
| `tools/media/angles-area.mjs` / `angles-area2.mjs` | 都道府県ハブの記事20本ぶんの切り口（章立てと段落） |
| `tools/media/angles-industry.mjs`（14）／`angles-issue.mjs`（14）／`angles-dr.mjs`（12）／`angles-theme.mjs`（8） | 各グループの切り口 |
| `tools/media/deepen.mjs` ＋ `deepen-extra.mjs` | 全記事に足す共通パート（ハブ固有の前提／よくある誤解／自社に当てはめる／よくある質問／内部リンク） |
| `tools/media/common.mjs` | seed固定の乱数、使い回す段落バンク、都道府県の言い換え（道内／都内／府内） |
| `tools/media/render.mjs` | HTMLの組み立てとCSS。**CTAの文言はここ1か所**（`ctaBlock()`） |
| `tools/media/generate.mjs` | 生成器の本体。自己点検（slug重複・タイトル重複・本文の完全一致・文字数）を出す |
| `tools/media/first-seen.json` | 記事slug → 初出日。**消さない**（消すと全2,000本の公開日が再生成日に化ける） |

### カテゴリ構成（グループ5 / ハブ131 / 記事2,000）

| グループ | ハブ | 1ハブあたり | 計 |
|---|---|---|---|
| エリアから探す（47都道府県） | 47 | 20本 | 940 |
| 業種から探す | 22 | 14本 | 308 |
| 採用の課題から探す | 32 | 14本 | 448 |
| ダイレクトリクルーティング・OfferBox | 16 | 12本 | 192 |
| 地方採用のテーマ | 14 | 8本 | 112 |

⚠ **ハブを増やしたら `hubs.mjs` の `articles` をそのグループの切り口の数と一致させる。** 合わないと生成器が止まる。
課題・DR・テーマのハブを増やしたときは **`hub-points.mjs` にも足す**（無いと止まる）。

### ⚠ 記事の中身のルール

- **統計値を書かない。** 有効求人倍率・県内就職率・内定率などは年ごとに動く。静的サイトなので更新されず、
  古い数字が2,000ページに残り続ける。数字を出すのは自社の事実（料金20万円／完全成果報酬）だけ。
  `prefectures.mjs` に入れてよいのは、年で動かない事実（地域ブロック・県庁所在地・主要都市・主要産業・主要大学）まで。
- **実在企業の事例・実績として読める書き方をしない。** 記事末尾に「支援で得た知見をもとに構成した実務向けの解説」と明記している（`DISCLAIMER`）。
- **乱数は seed 固定。** 再生成しても同じ記事になるので差分レビューができる。
  ⚠ ただし**バンク（`common.mjs` / `deepen*.mjs`）に文面を足すと配列長が変わり、既存記事で選ばれる文が入れ替わる。**
  公開後に増やすときは、再生成して差分を確認してから push する。
- 北海道・東京都・大阪府・京都府は、切り口側の「県内／県外」を `localizePref()` が道内／都内／府内に言い換える。

### 現状の課題（次に触るときに見るところ）

- **同じ切り口の記事どうしの重複率**（例: 北海道の採用市場 vs 青森県の採用市場）は 8-gram 実測で **約45〜50%**。
  ランダムな2記事の平均は約17%。切り口の章立てが全ハブ共通で、固定の段落（変種を持たない段落）が413あるため。
  下げるには `angles-*.mjs` の固定段落を `r.pick([...])` の複数変種に置き換えていくのが直接的（未着手）。
- GA4 等の解析は**入れていない**（LP本体にも入っていないため）。入れるならLPと合わせて判断する。
- 記事の公開日は全2,000本が立ち上げ日。日付を分散させる（先付け・後付け）ことはしていない。

### 見た目

- LPと同じトークン（白ベース／ネイビー×ティール、ダークモード無効）。**LPの配色を変えたら `render.mjs` の `CSS` も合わせる。**
- 外部CDNは読まない（LPと同じ方針）。ただし2,000ページで同じCSSを重複させないため、
  **CSSだけは `/media/assets/media.css` に切り出している**（`render.mjs` の `CSS` 定数が原本）。
- ロゴ・ファビコンはルートの `logo.png` / `favicon.ico` / `icon-192.png` / `apple-touch-icon.png` を参照。

### LPからの導線

`index.html` のナビ右側「採用ノウハウ」とフッター「地方採用ラボ（採用ノウハウメディア）」から `/media/` へ。
⚠ **ナビは幅430px以下でリンクを隠している**（隠さないとCTAボタンが押し出される）。

### Search Console

`sitemap.xml`（約2,140URL）を**登録すること**。`robots.txt` から参照している。

---

---

## 採用企業向け（B2B）LP 共通ルール

このリポジトリは**採用企業向けB2B LP 4本**のひとつ。4本は 2026-08-11 に共通のデザインシステムで作り直した。

| ドメイン | リポジトリ | サービス | アクセント色 |
|---|---|---|---|
| scout.agent-best.net | agentbest/scout | スカウト代行（主要媒体全般） | インディゴ #4C4FD8 |
| green.agent-best.net | agentbest/green | Green運用代行（1面談2万円の面談課金） | グリーン #12A150 |
| infra.agent-best.net | agentbest/infra | Infra 長期インターン採用代理店 | アンバー #C2610C |
| scoutdaikou-offerbox.agent-best.net | agentbest/scoutdaikou_offerbox | OfferBox運用代行 | ティール #0D7E93 |

### 共通の型（新規B2B LPはこれをコピーする）

- `index.html` **1ファイル完結**。CSS・JS・アイコン（インラインSVG）をすべて内包し、**外部CDNを一切読まない**。
- ロゴとファビコンだけは画像ファイル（同ディレクトリの `logo.png` / `favicon.ico` / `icon-192.png` / `apple-touch-icon.png`）を参照する。原本は `C:\Users\user\ロゴ素材\`（生成スクリプト `make_assets.py` 付き）。
- CSS変数トークン（`--paper` / `--ink` / `--accent` / `--navy` 等）＋ **ネイビーのヒーロー＆最終CTA**。アクセント色だけ差し替える。
- `.rv` クラス＋IntersectionObserverでスクロール表示。JS無効時の保険に `<noscript>` で `.rv{opacity:1}`。
- canonical・OGP・favicon（ABマークのSVG data URI）・robots を自ドメインで設定。
- フッターに4本の相互リンク。

### ⚠ 問い合わせ窓口ルール（松岡さんの指示・2026-08-11）

- **CTAから日程調整ツール（Timerex／Calendly）へいきなり遷移させない。** ページ内のお問い合わせフォームで受け、日程調整リンクは**送信後のサンクス画面にだけ**置く。
- フォームはコーポレート `https://www.agent-best.net/contact` と**同一のGoogleフォーム**を使う（フォームID・entry IDは `agentbest/agentbest-lp` の `src/pages/contact.astro` にある）。項目も同じ4つ＝会社名／お名前／メールアドレス／お問い合わせ内容。
- `fetch` の `mode:'no-cors'` で `formResponse` にPOSTし、成功したらサンクス表示に差し替える実装。
- 4LPが同じフォームに入るので、**送信本文の末尾に「お問い合わせ元：〇〇LP」を自動付記**して判別する。新しいB2B LPを作るときも同じ型をコピーし、`SOURCE` 定数だけ変える。
- **求職者向けLPのCTA（Calendly直リンク）とは別ルール。** 混ぜないこと。

### ⚠ Genspark残骸の罠

infra の旧版には Genspark のバッジスクリプト、**他サイトのCloudflare Insightsトークン**、Unsplashホットリンクが埋まっていた（Genspark生成ページの残骸）。同種の生成物を扱うときは同じ混入を必ず疑い、grepで確認する。

## push のルール

- ローカルで動作確認（ブラウザ表示・構文チェック）を済ませた変更は、**確認を取らずに commit & push してよい**。コミットメッセージは日本語。**push後は必ず何を変えたか報告する**（無言でpushしない）。
- **以下に触れるときは必ず止まって事前確認する**:
  1. ドメイン・DNS・CNAME（DNSは**Squarespace Domains**管理・松岡さんの手作業）
  2. 個人情報・フォーム・認証
  3. 費用が発生する変更
  4. 既存ページ・データの削除
  5. 複数リポジトリへの一括変更
- Publicリポジトリ。push前にトークン・APIキーの混入をgrepで確認する。
- `main` への push = **即本番公開**（GitHub Pages）。
